Include /Users/saegey/.ssh/conductor_config

# Added by OrbStack: 'orb' SSH host for Linux machines
# This only works if it's at the top of ssh_config (before any Host blocks).
# This won't be added again if you remove it.
Include ~/.orbstack/ssh/config

Include /Users/saegey/.colima/ssh_config

# Host *
#   AddKeysToAgent yes
#   UseKeychain yes
#   IdentityFile ~/.ssh/id_rsa

Host *
	IdentityAgent "/Users/saegey/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"

# Amazon Q SSH Integration. Keep at the bottom of this file.
Match all
  Include "~/Library/Application Support/amazon-q/ssh"

Host grooovenet
  HostName ssh-grooovenet.vinylplay.cc
  User ben
  ProxyCommand cloudflared access ssh --hostname %h
